export const selectAccount = (state) => state.account.data;
export const selectAccountLoading = (state) => state.account.loading;
export const selectAccountStats = (state) => state.account.stats.data;
export const selectAccountStatsLoading = (state) => state.account.stats.loading;
